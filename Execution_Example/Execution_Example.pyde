# The setup function
# Only runs ones when we start the application
def setup():
    print("This runs one")
    
# The draw function
# This function is called over and over again
# It keeps running until the applications ends
def draw():
    print("This draws multiple times")
